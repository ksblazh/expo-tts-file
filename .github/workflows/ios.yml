name: iOS build

# Compiles the Swift the TS gate never touches, through the example app that autolinks
# the module. React Native is built from source here (the SDK 56 precompiled-XCFramework
# bug documented in the README), which takes ~20 minutes — so this runs on main and on
# demand rather than on every PR. To check a branch before merging:
#   gh workflow run ios.yml --ref <branch>

on:
  push:
    branches: [main]
    paths:
      - 'ios/**'
      - 'example/**'
      - 'expo-module.config.json'
      - 'package.json'
      - '.github/workflows/ios.yml'
  workflow_dispatch:

concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios:
    name: iOS (Swift)
    runs-on: macos-latest
    timeout-minutes: 90
    env:
      RCT_USE_PREBUILT_RNCORE: '0'
      RCT_USE_RN_DEP: '0'
      EXPO_USE_PRECOMPILED_MODULES: '0'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: example/package-lock.json
      - name: Install example dependencies
        working-directory: example
        run: npm ci
      - name: Prebuild the iOS project
        working-directory: example
        run: npx expo prebuild -p ios --no-install
      - name: Install pods
        working-directory: example/ios
        run: pod install
      - name: Build for the simulator
        working-directory: example/ios
        run: |
          WORKSPACE="$(ls -d *.xcworkspace | head -1)"
          SCHEME="$(basename "$WORKSPACE" .xcworkspace)"
          echo "Building $SCHEME from $WORKSPACE"
          xcodebuild \
            -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build
